# Password strength checker

Checks password strength for length, lowercase, uppercase, special charater and not repeated using python *Regex* library, checks if password is in weak_passwords.txt

### Run
```bash
python logic.py
