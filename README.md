# Password strength checker

A simple Python script to evaluate password strength using Regex and common dictionary checks.

## Features
- Length and character set validation (upper, lower, digits, symbols)
- Sequential repeated character detection
- Common password lookup via `weak_passwords.txt`
- Custom scoring and improvement suggestions

## Usage
Ensure `weak_passwords.txt` is in the same folder, then run:

```bash
python logic.py
